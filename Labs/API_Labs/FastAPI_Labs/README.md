Hi,

I have implemented a simple train-test model based on the california housing dataset. 

It loads the dataset and does the usual job. I have also created a docker image of this to be stored on gcp and deployed using cloudrun.
