Here, I have changed the front end UI to be for the California Housing Price Prediction

The user can input the feature variable values either using a manual entry or can upload a test file having all these values. 

It just gives a interactinve UI where the values can be seen.